[build-system]
requires = ["setuptools>=61", "wheel", "build"]
build-backend = "setuptools.build_meta"

[tool.pixi.workspace]
name = "steve_SigProX"
channels = ["conda-forge"]
platforms = ["linux-64"]

[tool.pixi.pypi-dependencies]
steve_SigProX = { path = ".", editable = true }

[tool.pixi.tasks]
